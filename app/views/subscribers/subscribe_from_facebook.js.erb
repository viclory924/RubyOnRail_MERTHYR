var body = 'Voucher: <%= public_voucher_url(@subscriber.deal)-%>';

//FB.login(function(response) {
  FB.api('/me/feed', 'post', { message: body }, function(response) {
    if (!response || response.error) {
      alert('Error occured');
    } else {
      alert('Post ID: ' + response.id);
    }
  });
//});
